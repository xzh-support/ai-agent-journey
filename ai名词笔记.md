# AI 学习名词笔记

> 按"问题 → 催生下一个方案"的演进逻辑整理。学完一个阶段回来补充细节。

---

## Token

最小信息单位，AI 处理文本时不可再分的最小单位。将输入文本拆分成一个个小片段，一个片段对应一个 Token。

Token 序列：按顺序排列形成 Token 序列，模型通过理解序列来处理语义。

- 实例："我想吃香蕉" → 拆为"我""想""吃""香蕉"（中文大模型通常 1 个汉字 ≈ 1 个 token）
- **计费按 token 算**——API 调用花钱的地方

## 上下文窗口（Context Window）

定义了模型单次能处理的最大 Token 数量，决定 AI 一次对话能看到多少内容。

- 不是窗口越大模型越聪明：内容过多可能被无关内容干扰，过少可能答不出来
- 关键是怎么把任务交代清楚 → 引出 Prompt

## Prompt

提示词，用户向 AI 传达任务意图的核心工具与表达方式。

- 早期直觉交互：简单输入就有答案、零门槛，但同一个问题不同问法结果差距很大
- 提问四要素：明确角色、指定任务、指定结构 → 输出更具体、更有结构、更接近预期

## Prompt Engineering

提示词工程，本质是给 AI 写一份清晰的"工作说明书"，系统告诉它角色、任务、标准。

工作说明书四要素：① 角色定位 ② 任务描述 ③ 背景信息 ④ 验收标准

- 提示词再清楚也无法完成模型不知道的东西
- 被问到训练数据中不存在的知识时，AI 产生**幻觉（Hallucination）**——一本正经编造看似合理实则虚假的回答 → 催生 RAG

## RAG（检索增强生成）

Retrieval-Augmented Generation。让 AI **先查资料再回答**：不从训练记忆回答，而是先从外部知识库检索相关资料，再基于资料生成答案。

- 企业内部文档、产品手册等私有知识从未出现在通用模型训练数据中，没有 RAG 时 AI 对此天然存在知识盲区
- 工作流程：存入知识库 → 检索相关内容 → 交给 AI → 基于资料回答（回答有据可查）
- 核心逻辑：先检索外部知识，再基于检索结果生成回答

## Embedding（向量化）

将自然语言文字转化为高维数字向量，使计算机可以用**数学运算近似语义相似度**。

- 把文字变成一串数字，映射到高维空间，每个词/句子对应一个坐标点
- 用向量距离近似语义：距离近 = 意思近（常用**余弦相似度**计算）
- 例："怎么申请退款" 和 "订单取消后钱怎么退" 字面不同但语义相近——比关键词搜索更智能

## 向量数据库

专门存储和检索 Embedding 向量的数据库（如 Chroma、FAISS）。

- RAG 的存储层：知识库内容 → Embedding → 存入向量库 → 查询时相似度检索

## Tool Calling / Function Calling

让 AI 从"只生成文字"进化到"能调用外部工具执行动作"，相当于给 AI 装上**手**，具备真正的行动能力。

- OpenAI 叫 Function Calling，Anthropic 叫 Tool Use——同一个东西的不同叫法
- 每个工具接口不同、接法各异，开发和维护成本高 → 催生 MCP

## MCP（模型上下文协议）

Model Context Protocol。AI 连接外部工具的**标准协议**，类比 AI 世界的 USB-C——统一接口标准替代碎片化连接。

- 以前接工具像焊电线，每个 AI 应用接入不同工具都要单独开发适配代码
- MCP 统一解决：工具暴露方式、参数传递规范、调用结果返回、权限安全控制
- 让 AI 从聊天框进化为可接入各种插件和工具的系统平台

## Context Engineering（上下文工程）

影响 AI 效果的不只是模型和工具，还有**它当时看到了什么信息**——每次任务时系统为它准备了哪些信息。

- Prompt Engineering 不是过时而是不够用：从简单问答走向复杂场景，单条指令无法管理系统级的信息流设计
- 系统设计每次任务中 AI 应获取的信息组合：历史对话、用户资料、数据库结果、公司规范、工具状态
- 对比：Prompt Engineering = 写好一条指令（关注单次对话表达）；Context Engineering = 设计整个信息流（统筹全局信息架构）
- 核心不是给 AI 更多信息，是投喂**刚好需要**的信息——不多不少，恰到好处，避免不足/过载/过期

## Skill（技能）

每次任务都要交代相同要求极其低效 → 将特定 AI 能力一次性封装，后续反复调用。

- 解决"不想每次都重新教 AI 一遍"：把重复性任务的要求封装为可复用模块，一键调用
- 例：每周写周报的格式偏好固化为 Skill，下次一句话调用

## Agent

不是更聪明的聊天机器人，而是**能围绕目标自主拆解步骤、选择工具、观察结果并持续调整**的 AI 系统。

- 核心能力：围绕目标的自主规划——自己拆解步骤、选择工具、观察执行结果、根据反馈动态调整
- 三种 AI 递进：聊天机器人（被动回答）→ Tool Calling（执行单步动作）→ Agent（围绕目标自主推进）
- 编程场景天然适合 Agent：代码文件结构明确、报错信息具体、测试结果可验证、版本控制可追溯 → 完整自主执行闭环

## Vibe Coding

全新编程范式：程序员从写代码转变为**描述目标**，由 AI 生成实现，程序员负责验收和调整。

- 工作重心从写具体实现转移到：需求定义、任务拆解、架构设计、代码审核、AI Agent 管理

## Harness Engineering（安全约束系统）

Agent 越强大越自主，潜在风险越大：犯错、误删文件、生成不安全代码、执行跑偏、消耗过量资源。

- Agent 不是越自由越好，必须通过工程约束保障安全可靠
- Harness = Agent 的控制系统（像汽车的安全带、方向盘、刹车；模型是发动机）
- 涵盖：权限控制、执行沙盒、验证输出、人工审批、安全边界、回滚机制
- 例：让 Agent 清理文件，Harness 限制它只能访问指定文件夹
- **企业的真正需求**：不是"看起来聪明"的 Agent，而是安全可控、可追踪、能被验证的 Agent——AI 落地的难点从模型智能转到系统可靠

## Loop Engineering（自主闭环）

Harness 确保 Agent **不闯祸**，Loop 确保 Agent **能成事**——前者是安全约束，后者是自主闭环执行与持续纠错的能力。

- 自动导航和纠错系统：自己规划、自己改错
- 包含：目标拆解、自检复盘、自动重试、进度留存、达标判定、成本管控
- AI 落地新趋势：从拼系统安全走向拼任务闭环能力

## Workflow（N8N / Coze）

AI 怎么进入业务流程：真实业务是涉及多系统、多步骤的完整流程——表单提交 → 客户分类 → 销售分配 → 邮件发送 → 数据写入。

- Workflow 的价值不在增强模型，而在将 AI、API、数据库、消息系统、人工审批**串联为完整业务流程**

## Workspace Agent

Workflow 是一条流程，Workspace Agent 是**一个岗位助手**，长期待在团队里。

- 需要深度理解团队长期积累的全部上下文：项目、文档、分工、客户关系、敏感信息、审批流程

---

## 待补充（第二阶段学习时回来填）

- **Temperature**：随机性旋钮，0=严谨保守，1=天马行空。写代码低值，写文案高值
- **Few-shot**：在 Prompt 里给几个例子，让模型照葫芦画瓢
- **CoT（思维链）**：让模型"先想步骤再回答"，复杂推理题正确率大幅提升
- **Multi-Agent / ReAct**：第三阶段（Day 29-30）重点
