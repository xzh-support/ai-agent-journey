# 学习总结（截至 2026-08-15）

> 面试重要度标注：⭐⭐⭐ 必考/高频 | ⭐⭐ 常见 | ⭐ 偶尔问到
> 后续每学完一个模块，用同样的格式追加总结。

---

## 一、Python 基础（已完成 第2-9章）

### 1. 基础语法 ⭐⭐⭐

| 知识点 | 要点 |
|--------|------|
| 变量 | 无类型声明，变量随时可变类型；`type()` 查类型 |
| 类型转换 | `str()` `int()` `float()`；万物可转字符串，字符串不一定能转数字 |
| 标识符 | 只能中文/英文/数字/下划线，不能数字开头，大小写敏感，不能用关键字 |
| 运算符 | `+ - * / % //`（整除）`**`（指数）；复合赋值 `+=` `-=` 等 |
| 字符串 | 单引号/双引号/三引号；转义 `\`；f-string：`f"我是{name}"` |

**面试高频点**：
- `//` 和 `/` 的区别（整除 vs 浮点除法）
- f-string 格式化（比 `%` 和 `.format()` 现代）
- `int("11")` 能转，`int("黑马")` 报 ValueError

### 2. 流程控制 ⭐⭐⭐

```python
# while：条件循环，注意循环终止条件（i += 1 别忘！）
while i <= 100:
    i += 1

# print 不换行
print("你好", end="")    # end 默认是换行符 \n
```

### 3. 数据容器 ⭐⭐⭐（面试最爱考）

| 容器 | 标志 | 可重复 | 可修改 | 有序 | 典型用法 |
|------|------|--------|--------|------|----------|
| list 列表 | `[]` | ✅ | ✅ | ✅ | 存任意序列 |
| tuple 元组 | `()` | ✅ | ❌ | ✅ | 不可变数据 |
| str 字符串 | `""` | ✅ | ❌ | ✅ | 文本 |
| set 集合 | `{}` | ❌ | ✅ | ❌ | 去重 |
| dict 字典 | `{k:v}` | key不重复 | ✅ | ✅(3.7+) | 键值映射 |

**面试高频点**：
- 列表下标索引从 0 开始，反向从 -1 开始
- **元组"不可变"只锁元素引用**：`tuple1[2]` 指向的列表内部还能改（`tuple1[2][0] = None` ✅）
- 删除元素：`list.remove(值)` 按内容删、`del list[i]` / `list.pop(i)` 按下标删
- set 天然去重：循环 `add()` 即可

### 4. 函数 ⭐⭐⭐

```python
def 函数名(形参):
    return 值    # 没有 return 默认返回 None

# 多个返回值（本质是元组）
return 1, 2, 3

# 关键字参数：顺序不重要
test(name="xzh", age=11)

# 缺省参数：有默认值，必须放最后
def test(name, age, gender="男"): ...

# 不定长参数
def f(*args, **kwargs): ...
```

**面试高频点**：缺省参数放最后、`*args` 收元组 `**kwargs` 收字典

### 5. 文件操作 ⭐⭐

```python
f = open("路径", "r", encoding="UTF-8")   # r读 w写 a追加
print(type(f))    # <class '_io.TextIOWrapper'>
```

**Windows 路径坑**：`D:/文件夹/文件`（正斜杠），不是 `D:/盘符:/文件`

### 6. 异常 + 模块 ⭐⭐

- `try/except` 捕获异常
- 包 = 文件夹 + `__init__.py`
- **导入路径问题**：Python 默认只在脚本所在目录找包。PyCharm 会自动加项目根目录，VS Code 需要配 `.env` + `PYTHONPATH=.`

### 7. 装饰器 ⭐⭐

```python
@log          # 给函数套壳：不改原函数，增加额外功能
def f(): ...
```

- 能看懂 `@app.post()`、`@tool` 即可，原理以后深究
- 注意：黑马课程里可能放在高级部分讲

---

## 二、Git ⭐⭐⭐（Day 6，已完成）

### 日常工作流（每天用的三连）

```bash
git add .                    # 暂存所有变更（放进购物车）
git commit -m "说明"         # 保存快照到本地仓库（结账）
git push origin main         # 推送到远程（快递发出）
git status                   # 看当前状态（有没有东西没提交）
git log --oneline --graph    # 看提交历史（左边是哈希值缩写）
```

### 核心概念（面试必考）

**三个区域**：

```
工作区 ──git add──▶ 暂存区 ──git commit──▶ 本地仓库 ──git push──▶ 远程仓库
(改代码)            (购物车)              (提交历史)              (GitHub/Gitee)
```

**main vs origin/main（高频考点）**：

| 概念 | 说明 |
|------|------|
| `main` | 本地分支，`git commit` 就往前走 |
| `origin/main` | 本地对远程状态的**备忘录**，只有 push/pull/fetch 时才更新 |
| "ahead of origin/main by 1 commit" | 本地 commit 了但还没 push，备忘录落后一个提交 |
| `pull` = `fetch` + `merge` | 先更新备忘录，再合并到本地 main |

**提交哈希**：每个提交的唯一 ID（40位），`git log` 显示前 7 位缩写，checkout/reset 时用。

### 分支操作

```bash
git branch dev              # 创建分支
git switch dev              # 切换分支（checkout 也行）
git checkout -b dev         # 创建 + 切换二合一（-b = branch）
git branch dev C2           # 创建分支并直接指向指定提交
git branch -f main C2       # 强制移动已有分支指针（-f = force）
git merge dev               # 合并 dev 到当前分支
```

**记忆**：`-f` 移动已有分支，创建新分支时直接 `git branch 名字 位置`。

### 回退（面试高频）

| 命令 | 历史记录 | 结果 | 适用场景 |
|------|---------|------|---------|
| `git reset C2` | C3 从历史消失 | 像 C3 从没发生 | 本地，还没 push 的提交 |
| `git revert C3` | C3 保留，新增 C4 | 内容回到 C2 状态 | 已 push 到远程（安全） |

**revert 的本质**：新提交 C4 = 被撤销提交 C3 的"反操作"（C3 加什么 C4 就删什么）。

### 远程

```bash
git clone 地址              # 下载远程仓库到本地
git push 远程名 分支名       # 上传（本地 → 远程）
git pull                   # 拉取并合并（= fetch + merge），多人协作用
git fetch                  # 只下载更新备忘录，不动本地代码
```

**本人配置**：两个远程仓库
- `origin` = Gitee（gitee.com/xu-zhenhui1/ai-agent-journey）
- `github` = GitHub（github.com/xzh-support/ai-agent-journey）
- 每次推两条：`git push origin main && git push github main`

### 闯关经验（Learn Git Branching）

- 必学：基础关 1-12（branch/merge/checkout/reset/revert）+ 远程前 5-6 关（clone/push/pull/fetch）
- 暂缓：多人协作模拟、远程 rebase、冲突处理进阶——团队项目时再学
- `git branch` 没有 `-i` 选项；`^` 是上1个提交，`~3` 是往前数3个

### 踩过的坑

- 手滑多打一个 `git`（`git git push` ❌）
- commit 完以为"上传了"——commit 只是本地保存，**push 才算上传**
- `git add .` 后 "nothing to commit, working tree clean" = 之前已全部提交过，正常

---

## 三、Linux 基础 ⭐⭐⭐（用 Git Bash 练）

### 查看文件

```bash
cat 文件        # 打印全部
head -5 文件    # 前5行
tail -3 文件    # 后3行
wc -l 文件      # 数行数
```

### 搜索 grep

```bash
grep 关键词 文件      # 找含关键词的行
grep -c ERROR 文件    # count 计数
grep -n INFO 文件     # number 显示行号
grep -v INFO 文件     # invert 反转（不含）
grep -i error 文件    # ignore case 忽略大小写
```

### 管道与重定向 ⭐⭐⭐（灵魂考点）

```bash
grep ERROR app.log | wc -l    # | 管道：左边结果喂给右边
grep ERROR app.log > e.txt    # > 覆盖写入文件
grep WARN app.log >> e.txt    # >> 追加（不查重！）
```

### 表格处理

```bash
cut -d, -f1 file.csv              # 按逗号切列，取第1列
sort -t, -k2 -nr file.csv         # 按第2列数字倒序
awk -F, '{print $2}' file.csv     # awk 通用写法
sort -u 文件                      # unique 去重
```

### 其他

```bash
find . -name "*.py"     # 按名字找文件
find . -type d          # 只找目录
chmod 755 script.sh     # 权限：7=rwx 5=r-x（4读2写1执行）
ps aux | grep python    # 查进程
kill 进程号              # 杀进程
top                     # 实时资源监控，q退出
```

**路径注意**：Git Bash 里 `D盘 = /d/`，用正斜杠

---

## 四、Docker ⭐⭐⭐（Day 8，已完成 2026-08-15）

### 核心概念（面试必答）

| 问题 | 答案 |
|------|------|
| Docker 解决什么 | "我电脑能跑你电脑不行"——把代码+环境打包成标准盒子，任何机器一致运行 |
| 镜像 vs 容器 | 镜像是模板（类），容器是运行实例（对象）；镜像 build 出来，容器 run 出来 |
| 容器 vs 虚拟机 | 容器共享宿主内核，秒级启动、几十MB起；虚拟机模拟整套硬件，分钟级、几个GB |
| 上传方式 | 传 Dockerfile+代码（团队协作）；或 `docker push` 传镜像（发布用） |

### 核心命令

```bash
docker build -t 名字:版本 .          # 按 Dockerfile 造镜像（. 是配方目录）
docker run -d --name 名字 -p 外:内 镜像   # 开容器
#   -d 后台  --name 命名  -p 端口映射（电脑端口→容器端口）

docker images / docker ps / docker ps -a   # 看镜像 / 运行中容器 / 全部容器
docker logs 名字 / docker exec -it 名字 bash  # 看日志 / 进入容器内部
docker stop / start / rm 名字              # 停 / 启 / 删
```

### Dockerfile 语法（面试高频）

```dockerfile
FROM python:3.11              # 基础镜像（地基）
WORKDIR /app                  # 工作目录（进门）
COPY hello.py .               # 复制文件进容器（搬家具）
CMD ["python", "hello.py"]    # 启动命令（入住后干嘛）
```

**镜像分层**：每行指令一层，未变化的层下次 build 直接缓存（这就是为什么第二次 build 只要 2.7 秒）

### docker-compose ⭐⭐

```yaml
services:
  app:
    build: .                  # 用 Dockerfile 构建
    ports: ["9000:8000"]
  redis:
    image: redis:7            # 直接用现成镜像
```

```bash
docker compose up -d          # 一键启动所有服务（自动建网络）
docker compose down           # 全部停止删除
docker compose ps / logs / restart / up -d --build
```

**核心价值**：多服务项目（FastAPI+PostgreSQL+Redis）一次启动；同一 compose 网络内，容器直接用服务名互相访问（如 `redis:6379`）

### 踩过的坑

- 国内拉大镜像超时 → Docker Desktop → Settings → Docker Engine 配 `registry-mirrors`
- 旧版 `docker-compose`（带横杠）= 新版 `docker compose`
- Git Bash 在装 Docker 前打开的窗口识别不了 docker 命令，重开一个

---

## 五、数据库 ⭐⭐⭐（Day 9-11，已完成 2026-08-16）

### 5.1 SQL 强化（MySQL 8.0 实操）

**JOIN 选择决策法**：

```
1. 问："主角"是谁（学生/课程/班级...）
2. 主角每行都必须保留（哪怕没匹配）→ LEFT JOIN（主角放左边）
   只关心有交集的数据 → INNER JOIN
3. 不用 RIGHT JOIN：永远 LEFT JOIN + 换表顺序
```

**ON vs WHERE（高频陷阱）**：ON 管连接条件（连接时生效），WHERE 管结果过滤（连接后生效）。LEFT JOIN 时把条件从 ON 挪到 WHERE 会让它退化成内连接 —— "LEFT JOIN + 附加筛选"时筛选右表的条件必须写 ON 里。

**子查询三种位置**：
- WHERE 里：必须返回一行一列（标量），`score > (SELECT AVG...)`
- FROM 里：派生表 `FROM ( ... ) t`，窗口函数列必须先算出来外层才能筛
- SELECT 里：每行算一次

**窗口函数**：

| 函数 | 并列时 | 用途 |
|------|--------|------|
| ROW_NUMBER | 不并列（纯流水号） | "取前 N 个"（数量语义） |
| RANK | 并列跳号（1,1,3） | 排名（名次语义） |
| DENSE_RANK | 并列不跳号（1,1,2） | 排名 |

语法：`函数() OVER (PARTITION BY 分组 ORDER BY 排序)`。与 GROUP BY 共存时：GROUP BY 先折叠，窗口函数后加列。窗口函数里不能嵌套聚合函数。

**索引（B+树）面试标准答案**：
- 为什么 B+ 树：磁盘 IO 决定性能；矮胖（一节点一页装大量键值），百万数据 3 层；数据只在叶子，中间层全路标；叶子链表串起来 → 范围查询高效
- 聚簇索引（主键，叶子存整行）vs 二级索引（叶子存主键值，要**回表**）
- 自增主键好：新数据总插最右边，树不频繁分裂
- EXPLAIN 看 `type`：const/eq_ref > ref > range > ALL（全表扫描最差）

**索引失效场景（面试高频）**：列上做运算（`student_id+0=1`）、左模糊 `LIKE '%x'`、隐式类型转换、联合索引不满足最左前缀。EXPLAIN 实测：建索引后 type ALL→ref，rows 14→3。

**老式隐式连接** `FROM a,b WHERE a.x=b.x` = 内连接，但做不了 LEFT JOIN，企业统一用显式 JOIN。

### 5.2 Redis（docker 实操）

**五种类型 + Agent 项目用途**：

| 类型 | 命令 | Agent 用途 |
|------|------|-----------|
| String | SET/GET/INCR/SETEX | 会话状态、验证码、限流计数 |
| Hash | HSET/HGET/HGETALL | 缓存用户对象、配置参数 |
| List | LPUSH/LRANGE/LPOP | **对话历史**（LPUSH 新消息，LRANGE 取最近 N 条拼 Prompt） |
| Set | SADD/SINTER/SUNION | 去重、标签 |
| ZSet | ZADD/ZRANGE/ZREVRANGE | 排行榜、按分选工具 |

**缓存三兄弟（面试必考）**：

| 病名 | 症状 | 对策 |
|------|------|------|
| 穿透 | 查不存在的数据，Redis 永远没有，全打 MySQL | 空结果缓存 + 布隆过滤器 |
| 击穿 | 单个热点 key 过期瞬间，海量请求直击 MySQL | 互斥锁重建 / 永不过期+逻辑过期 |
| 雪崩 | 大量 key 同时过期 | 过期时间加随机值 |

**分布式锁**：`SET lock key NX EX 10`（NX 抢锁 / EX 防死锁），干完 DEL，删前验证 token 防误删。与 PV 操作同思想，多三个难题：崩溃（EX 兜底）、跨机（Redis 当裁判）、误删（token 验证）。

**踩坑**：redis-cli 中文显示 `\xe5...` 是原始字节，加 `--raw` 参数解决；Redis 大小写敏感（"AI"≠"ai"）。

---

## 六、待学

- [ ] HTTP + 网络（Day 12-13）
- [ ] FastAPI 实战（Day 14-16，含 PostgreSQL + SQLAlchemy）
- [ ] LLM / Agent 相关
